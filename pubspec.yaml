name: workout_tracker
description: 筋トレワークアウト記録アプリ - トレーニングの記録、分析、支援ツールを提供
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.8
  provider: 6.1.5+1
  shared_preferences: 2.5.3
  intl: 0.19.0
  uuid: 4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
